version: '3.8'

services:
  auth-app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./auth.db:/app/auth.db
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
    restart: unless-stopped